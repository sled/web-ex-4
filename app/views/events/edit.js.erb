(function($) {
  // show form 
  $('#event-<%= @event.id %> .panel-body').html("<%= j render partial: 'form', locals: {group: @group, event: @event} %>");
  $('#event-<%= @event.id %> .panel-heading').hide();
})(jQuery);